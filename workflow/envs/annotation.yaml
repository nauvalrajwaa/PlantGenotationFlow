channels:
  - bioconda
  - conda-forge
dependencies:
  - liftoff    # Sesuai instruksi: conda install -c bioconda liftoff
  - minimap2   # Dependency Liftoff
  - gffread    # Untuk generate protein fasta dari GFF3
  - python>=3.8
  - numpy      # Sering dibutuhkan Liftoff
  - networkx   # Sering dibutuhkan Liftoff